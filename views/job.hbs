<h1>Batch: {{job._id}} ({{job.imageCount}} images)</h1>

<p>Submitted {{job.date}}.</p>

{{#if job.processed}}
    <p class="job-processed">Entire batch has been processed.</p>
{{/if}}

{{#each images}}
    <div style="float:left;width:150px;height:150px;">
        <img src="/images/{{fileName}}.jpg" style="max-width:150px;max-height:130px;"/>
        {{entropy}}
    </div>
{{/each}}

<ul class="clusters">
{{#each clusters}}
    <li class="cluster">
        {{#each images}}
            <div class="image">
                <a href="/cluster/{{../_id}}" target="_blank">
                    <img src="/images/{{.}}.jpg"/>
                </a>
                <div class="caption">{{.}}</div>
            </div>
        {{/each}}
    </li>
{{/each}}
</ul>

{{#if processed}}
<h2>Processed Groups</h2>

<ul class="clusters">
{{#each processed}}
    <li class="cluster processed">
        {{#each images}}
            <div class="image">
                <img src="/images/{{.}}.jpg"/>
                <div class="caption">{{.}}</div>
            </div>
        {{/each}}
        <div class="process">
            <span class="processed">(Already processed.)</span>
        </div>
    </li>
{{/each}}
</ul>
{{/if}}